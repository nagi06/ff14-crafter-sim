# 検証ノウハウ引き継ぎ書 — aqueduct製作シミュレーター

**対象読者**: このリポジトリを別環境（別PC・別セッション・別AIモデル）で検証・改修する人（またはClaude）。
コードはGitHubが正。この文書は「コードに書いていない判断・検証手法・確定済み事実」の引き継ぎ。
最終更新: 2026-08-23（v6.4時点・実戦0勝6敗まで反映）

---

## 1. システム構成と設計思想

- `aqueduct/index.html`: エンジン＋UI一体の単一ファイル。**ゲームロジックは全部この中の`<script>`**
  - 実行系: `doAction`/`endStep`/`canUse`/`cpCost`/`durCost`/`progGain`/`qualGain`
  - v6探索: `simApply`(副作用なしステップ関数)/`searchStep`(expectimax)/`evalSearch`(ゴール逆算評価)/`recommendSearch`
  - v5ヒューリスティック: `recommendCore`(生ドクトリン)/`recommend`(wouldDie安全網つき)。**探索の答えが違法・即死のときのフォールバック先**
- `aqueduct/mc/mc.mjs`: ヘッドレスMCハーネス。**index.htmlを無改変で読み込む**（`<script>`抽出＋DOMスタブ＋`COMBO_FLOOR`の1箇所置換のみ）。
  worker_threads並列（`--jobs`、シリアルと結果完全一致）
- `aqueduct/mc/calib.cjs` + `traces_*.txt`: 実戦トレースから状態出現率・ギャンブル成功率を集計する校正装置

**思想**: 正しさは「読んで納得」ではなく**実行検証**で担保する。修正のたびに下のパリティ群を回す。
エフォートは中で足りる（実行検証が正しさを担保する構図なら、賢さより検証回数）。

## 2. 検証スタック（修正したら必ず回す）

```
cd aqueduct/mc
node mc.mjs --parity                                # 13項目+状態分布。数秒
node mc.mjs --simparity 200 --seed 42 --runs 1      # simApply vs doAction 全フィールド突合(約5,000手)。数秒
node mc.mjs --runs 800 --brain search --depth 3 --seed 7 --out <name> --jobs 10   # スモーク。約80秒
node mc.mjs --runs 500 --brain search --depth 5 --seed 42 --out <name> --jobs 10  # 公式値。約25分
```

- **階層プロトコル**: 合法性・バグ修正系＝パリティ+simparity+スモーク800本で合格。
  勝率が動くはずのドクトリン・重み変更＝5,000本×2シード（seed 42/7）
- **シード設計が肝**: run i のシードは `seedBase+i` 固定。並列分割やCRN（閾値間共通乱数）でも
  結果がビット単位で再現される。**「旧コードとの完全一致」を等価性証明に使える**（並列化検証で実績）
- スモークの読み方: Wilson 95%CIが重なってれば「壊してない」。±2pt程度は雑音。
  `artifacts`（turn_cap等のハーネス打ち切り）が急増したら挙動が壊れてる兆候
- 実験フラグ: `--w key=val`(評価重み) / `--ban action`(封印) / `--gate action:cond1,cond2`(状態ゲート) / `--beam r,i` / `--depth n`

## 3. 実機仕様の確定事項（再調査不要・出典つき）

- **品質式**: `floor(baseQual × iqMult × eff × cond × mult)`——**内側で切り捨てない**（端数は最後まで保持）。
  2026-08-23実戦ログで確定: IQ2集中加工1096=348×1.2×1.5×1.75、IQ6下地加工1113=348×1.6×2（v6.4）
- **ヘイスティ成功率60%**（デアリングも60%、安定+25pt）。**Lv96特性の85%はコスモでは乗らない**。
  実測: ヘイスティ単体28/49=57.1%（85%仮説はp<1e-7で棄却）。「本家知識より実測データを信じる」の代表例
- 状態出現率: 352サンプル校正値（CONDITIONS定数のコメント参照）。頑強→翌ターン強制頑丈（自然出現の集計から除外すること）
- 高品質時の品質1.75倍はコスモツール(スターズ)効果。ダイスは140回検定で潔白（2026-08-18）
- QIはイノベ効果中使用不可／勢いはヘイスティ失敗ターンで消滅／完成判定は耐久0判定より先／
  マニピュは掛けたターン回復せず・耐久0を救えない／グレスト・確信は成功時のみ消費——すべて実装済み・監査済み
- **合法性監査は2026-08-18に3モデル（Fable/Opus/Sonnet）で完了**。Sonnetはteamcraft公式シミュのソースと突合済み。
  この系統の再監査は新パッチでルールが変わらない限り不要

## 4. バグ博物館（同型を再発させないための過去事例）

| バグ | 症状 | 教訓 |
|---|---|---|
| QI×イノベ(v6.1) | イノベ中にQI推奨→実機で押せない | canUse一箇所直せば探索/v5/UI全経路に効く構造を保て |
| 勢い残存(v6.2) | ヘイスティ失敗後もデアリング解禁 | endStepの減衰免除はアクションIDでなく「成否」も見る |
| 探索の自殺(v6.3) | 詰み盤面でビエルゴ即死を推奨 | **地平線効果**: 全ライン死ぬと「一番品質の高い死」が期待値最大になる。recommendSearchにwouldDie網＋評価関数の幻益（撃てないパックの計上）カットで対処。MC死因1位「耐久死」の大半がこれだった（修正で3手勝率10%→21%） |
| 品質端数(v6.4) | 全タッチが1〜2過小 | 実機ログとの1点単位突合を校正に使う |
| 神業スパムロック(v5.3) | 推奨が無限ループ | ユーザー目視も検出器のうち |

**共通教訓**: テストで見つからないのは「本家仕様の知識がないと気づけない」クラス。
新しい目（別モデル・実機ログ・ユーザーの違和感）が最強の検出器。ユーザーの「なんかおかしい」は必ず定量検証で受ける。

## 5. 棄却済み実験（再提案しないこと・すべてMC実測）

| 仮説 | 結果 | 結論 |
|---|---|---|
| 三段コンボ活用 | 閾値スイープ単調（封印=最適） | COMBO_FLOOR=9999のまま |
| IQ積み加工→洗練化 | 序盤耐久ドレイン倍で先に死ぬ | 倹約加工が正解 |
| 絶技温存(rTP 200→1200) | 単調悪化 9.75→8.6% | 200のまま。中盤消費は損じゃない |
| 突貫封印 | 20.1%→5.4%（1/4） | 0CP進捗がCP→品質全振りの源泉 |
| 突貫の状態窓ゲート | 20.1%→14.6% | 待機コスト>窓の得。探索は状態補正を都度織込済み |

## 6. 現在の性能と実戦記録（2026-08-23時点）

- 機械勝率: **5手33.4%**（95%CI 29.4-37.7・500本）・完走率98.2%・品質中央値28,986。3手20.1%（v6.4スモーク）
- 深さと1手の思考: 3手ほぼ一瞬／4手0.1秒／5手0.5秒（UI既定）／6手2-3秒
- MC実行時間の目安: depth3=0.1秒/本・depth5=2.6秒/本（--jobs 10で1/7）
- 実戦: 練習13本目で初勝利（31,520カンスト）。本番（実素材）0勝6敗
  27,697/30,144/29,839/30,567/22,181/30,069——**6本中5本が機械中央値超え＝人間上乗せは実在**。
  0/6の確率8.7%、単なる下振れ。敗因の運以外の全容疑者（コード・式・ドクトリン・ダイス）はアリバイ確認済み

## 7. 実戦運用プロトコル

1. 開始前: 飯・薬→**製作画面のCP最大値791を目視**（飯抜き事件の再発防止）→バッジ確認→5手→スタートライン→最初から
2. 製作中はファイル・リポジトリを触らない（パッチは終了後）
3. 節目相談: IQ10到達／工数残3,500／CP200切り／違和感——履歴トップ行コピペ＋**実機CPを添える**
4. ツールと実機のCPがズレたら「リカバリー入力」パネルで即同期（2戦目でズレ28→ビエルゴ不発の実害あり。
   ズレの主因は状態入力の食い違いで、事後特定は不可能。防ぐのではなく同期で吸収する）
5. 詰み盤面での正解は「見切って完走」（品質未達完走は低確率で素材返却。耐久死は確定ロスト）

## 8. 落とし穴（新しい検証者がハマりやすい順）

1. **本家知識で実装を「直さない」こと**。コスモは本家と違う（ヘイスティ60%等）。実測ログ＞Wiki＞記憶
2. simApplyとdoActionは**同じロジックの二重実装**。片方だけ直すとsimparityが検出する（していなければ回してない証拠）
3. noStep技（最終確認・設計変更・一心・QI）はターン非消費・コンボ非切断・lastAction非更新。ターン数勘定に注意
4. 探索の将来状態は「通常」仮定（ルート直後の1遷移のみ8状態展開）。深読み時の状態依存戦略は過小評価される構造
5. MCのartifacts枠（turn_cap等）は死因ではなく測定系の吸収。急増したときだけ疑う
6. Grepでコメント行頭が`\`に見えるのは表示の癖。ファイルは正常（Readで確認済みの実績あり）
7. 大規模MCはまず800本スモークで方向を見る。5,000本×5手をシングルスレッドで回すと100分コース

## 9. 未解決・保留

- **要実機確認・最優先（Kimi3レビュー2026-08-23で浮上）: GSはタッチ失敗でも消費されるか**。
  現実装は「成功時のみ消費」。Opus/Sonnet監査は温存を本家どおりと判定したが実機未確認で、
  手持ちの実戦ログに「GS中タッチ失敗→次タッチ」の観測が一度もなく白黒不明。
  確認法: GS張る→ヘイスティ失敗時にバフ欄のGSアイコン残存を見るだけ。
  もし実機が消費するなら探索はGS中ギャンブルを甘く見積もっており（楽観系統誤差）、
  doAction/simApply両方の修正＋MC再計測が必要。確信×突貫失敗も同型（同時に確認）
- 要実機確認4件（安い順）: FA効果中の重ねがけ可否／長持続×FA=7T?／長持続×QI=イノベ3T?／
  noStep技を挟んだコンボ維持（加工→最終確認→中級加工が18CPか32CPか）
- 機械勝率33.4%は「T1からの全自動プレイ」の数字であり実戦勝率の実測値ではない（実戦は全手動＋人間上乗せ。
  トップページの「前半マクロ」記述は旧運用）。ビームが待ち手（経過観察等）を静的評価で落とす構造の改善はv7候補
  （直すなら0CP手のビーム枠確保をMC A/Bで。雑な固定ルール化は棄却実験2件が示すとおり逆効果）
- `noStepBuff`フラグ未使用（絶技定義に残存・実害なし・掃除案件）
- d6既定化はWeb Worker非同期化が前提（同期2-3秒で保留中）
- 評価関数のスケール不整合（生存葉=品質1:1 vs 完走葉=×finishShort-2000）は詰み盤面の往生際問題の残根。
  v6.3の安全網で実害は塞いだが、原理的解決はv7課題
